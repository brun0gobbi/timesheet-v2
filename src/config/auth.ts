export const AUTH_CONFIG = {
    // Senha simples em texto plano para facilitar (pode-se usar hash em produção real)
    // Mas para este uso local, está OK.
    MASTER_PASSWORD: "Poletto@2026",
    STORAGE_KEY: "timesheet_auth_token"
};
